<h1>GWEN test over GL</h1>
<p>Just a fast test, so the Opengl setup havent finished jet... for full project have to write the myGL class and create a new SFML2 main file. maybe GL can be put into an sf::View...</p>

it uses the SFML2 sample of gwen (just a fast test modification)

<p>compile: <code>"make"</code></p>
<p>run: <code>"SFML2_on_GL"</code></p>
<p>clean: <code>"make clean"</code></p>
